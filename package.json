{
  "name": "BEMGS-MONOREPO",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w web",
    "dev:api": "npm run dev -w api"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
